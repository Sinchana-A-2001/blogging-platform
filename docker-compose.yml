version: "3.8"

services:

  frontend:
    build: ./frontend
    container_name: blog-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: blog-backend
    ports:
      - "5000:5000"
